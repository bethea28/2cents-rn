# dependencies
node_modules/

# Expo & Metro
.expo/
dist/
web-build/
expo-env.d.ts
.metro-health-check*

# 🛡️ THE SECURITY SHIELD
.env
.env.*
!.env.example
# Track this specific file for EAS, but ignore other root jsons
!google-services.json
ios/GoogleService-Info.plist

# 🛡️ THE NATIVE SHIELD
# We ignore the heavy build folders, but NOT the android/ folder itself
android/build/
android/.gradle/
android/app/build/
android/local.properties
# We keep the keystore for consistent signing across machines
!android/app/debug.keystore

# Specifically ensure google-services.json is tracked in its native home
!android/app/google-services.json

# Native Keys & Certificates (Keep these rules)
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# debug & logs
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# typescript
*.tsbuildinfo

# temporary/example folders
app-example